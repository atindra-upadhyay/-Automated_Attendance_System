{
  "name": "e-attend-root",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend-ready && npm install --prefix frontend-ready",
    "start:backend": "npm start --prefix backend-ready",
    "start:frontend": "npm start --prefix frontend-ready",
    "start": "npm run install:all && concurrently -n backend,frontend -c blue,green \"npm run start:backend\" \"npm run start:frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.3"
  }
}
